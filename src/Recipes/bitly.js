const fetch = require('node-fetch');

/**
 *
 * @param url
 * @returns {String}
 */
function convert(url) {
    return 'bitly strategy here';
}

/**
 *
 * @type {{convert: (function(*): *)}}
 */
module.exports = {
    convert
};